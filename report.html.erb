<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Book Report</title>
    <style media="screen">
      ul {
        list-style: none;
        display: flex;
        border-bottom: 5px solid black;
      }

      li {
        border: 1px solid gray;
        margin: 10px;
        padding: 10px;
      }
    </style>
  </head>
  <body>
      <p>/ Name / Phone / Address / Position / Salary / Slack accnt / GitHub /</p>
    <% @people.each do |person| %>
      <ul>
        <li><%= person.name %> / <%= person.address %> / <%= person.position %> / <%= person.salary %> / <%= person.slack_account %> / <%= person.github_account %></li>
        <li>Random space for <%= person.name %></li>
      </ul>
    <% end %>
  </body>
</html>
